import { ThemeComponents } from "@chakra-ui/theme";

const FormLabel: ThemeComponents["FormLabel"] = {
  baseStyle: {
    fontSize: "sm",
    fontWeight: 600,
    lineHeight: "120%",
  },
};

export default FormLabel;
